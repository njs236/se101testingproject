var sumAndProduct = function (number1, number2) {
	var sum = calculateSum(number1, number2);
	var product = calculateProduct(number1, number2);
	// Must return the sum and product together;
	return [sum, product]; 
}